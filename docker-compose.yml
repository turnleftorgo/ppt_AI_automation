services:
  app:
    build: .
    ports:
      - "3015:3001"
    env_file:
      - .env
    volumes:
      - ./templates:/app/templates
      - ./core:/app/core
      - ./models:/app/models
      - ./static:/app/static
      - ./main.py:/app/main.py
    restart: unless-stopped
